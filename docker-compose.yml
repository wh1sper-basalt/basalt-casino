# BASALT Casino — run bot with one command
# Copy .env.example to .env and set BOT_TOKEN, ADMIN_IDS

services:
  bot:
    build: .
    env_file: .env
    environment:
      - DATABASE_PATH=/data/database2.db
    volumes:
      - ./data:/data
      - ./backups:/backups
    restart: unless-stopped
